# Abnik Ahilasamy Portfolio

Static personal portfolio site for GitHub Pages.

## Structure

- `index.html` - home page
- `blogs.html` - blog index
- `projects.html` - projects index
- `blogs/` - static blog post pages
- `projects/` - static project detail pages
- `assets/css/styles.css` - shared styles and theme variables
- `assets/js/main.js` - light/dark theme toggle
- `assets/resume.pdf` - resume placeholder; replace with the real resume PDF

## Local preview

Open `index.html` directly in a browser. No install step, build step, backend, or server is required.

## Updating links

Replace placeholder project demo links, the email address, and the LinkedIn URL as needed. The GitHub link currently points to `https://github.com/kinba098`.

## Deploying to GitHub Pages

Commit these files to the GitHub Pages repository and enable Pages from the repository settings. The site is pure static HTML, CSS, and JavaScript.
